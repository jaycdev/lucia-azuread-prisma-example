export default function Home() {
    return (
        <p>This is an example repo, on using <a href="https://lucia-auth.com/">lucia-auth</a> with NextJS, Azure AD
            OAuth
            with
            Prisma.</p>)
}
